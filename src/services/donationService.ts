import type { DonationFormData } from '../types/donation';

// Submits a completed donation to the backend API.
export async function submitDonation(_data: DonationFormData): Promise<void> {
  // Replace with a real HTTP call once the API endpoint exists.
}
